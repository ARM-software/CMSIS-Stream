# CMSIS-Stream POSIX Hello

This example uses the POSIX runtime and the new high-level runtime API.

Build from this directory:

```sh
cmake -S . -B build
cmake --build build
```

The example is a regular CMake project and is intended to build on Linux,
macOS, and Windows.
