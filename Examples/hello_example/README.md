# README

Example of use of the CMSIS Stream runtime pack.

In this example, the dataflow graph is returning an error after some run. It is caught by the application event handler and the simulation stops.

